services:
  postgres:
    image: postgres:16
    container_name: marco-postgres
    environment:
      POSTGRES_USER: marco
      POSTGRES_PASSWORD: marco
      POSTGRES_DB: marco
    ports:
      - "5432:5432"
    volumes:
      - marco_pgdata:/var/lib/postgresql/data
volumes:
  marco_pgdata:
